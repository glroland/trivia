import React, {Component} from "react";
import "../App.css";

class GameConfig extends Component {

    constructor(props) {
        super(props);
        this.state = {
        };
    }

    render() {
        return(
            <p>GameConfig</p>
        );

    }
}

export default GameConfig;
